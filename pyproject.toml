[project]
name = "secret-tyrant"
version = "0.1.0"
description = "Secret Hitler board game implementation"
readme = "README.md"
authors = [
    { name = "Pramod Anandarao", email = "panandarao@wisc.edu" }
]
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
mcp = [
    "fastmcp>=3.4.0",
]

[build-system]
requires = ["uv_build>=0.8.6,<0.9.0"]
build-backend = "uv_build"

